#./install_dep_script

# install s3ql package
#dpkg -i debsrc/s3ql_1.12.0~natty1_amd64.deb
#python setup.py build_cython
#python setup.py build
#python setup.py install

#cp -rs /usr/bin/*s3ql* /usr/local/bin/

#
#-- Ovid Wu <ovid.wu@delta.com.tw> Tue, 24 Jul 2012 11:16:26 +0000
#
# add a private of APT repository server precise distribution
# add main & unstable components
cat >/etc/apt/sources.list.d/delta-server-precise.list <<EOF
deb http://www.deltacloud.tw/packages/ubuntu/ precise main
deb-src http://www.deltacloud.tw/packages/ubuntu precise main

deb http://www.deltacloud.tw/packages/ubuntu/ precise unstable
deb-src http://www.deltacloud.tw/packages/ubuntu precise unstable
EOF

sudo apt-get update
# import public key of Delta's Ubuntu apt server
sudo apt-key adv --recv-keys --keyserver keyserver.ubuntu.com 5FC23F2B
sudo apt-get -y install s3ql
# FIXME: install s3ql in /usr/local/bin in debian packaging (if it's required)
cp -rs /usr/bin/*s3ql* /usr/local/bin/
